# Unsloth Fine-Tuning Requirements
# Install with: pip install -r requirements-unsloth.txt

# Core Unsloth (install from git for latest version)
# unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Training dependencies (install without deps to avoid conflicts)
trl>=0.7.0
peft>=0.8.0
accelerate>=0.26.0
bitsandbytes>=0.42.0

# Base dependencies
torch>=2.1.0
transformers>=4.36.0
datasets>=2.16.0
xformers>=0.0.23

# Utilities
pandas>=2.0.0
numpy>=1.24.0
